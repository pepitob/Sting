<div class = "container">
  <div class="row justify-content-center">
    <div class="col-12 ">
      <div class="card welcome-mesage">
        <div class="user-pic"><%= cl_image_tag current_user.photo.key, class:"avatar-large" %></div>
        <div class="user-summary">
          <h2>Hello <%= current_user.first_name %>!</h2>
          <p>100$ total balance</p>
          <p>14km Run this week</p>
        </div>
      </div>
    </div>
  </div>

  <div class="row justify-content-center">
    <div class ="col-12">
      <div class="center-element"><h1>Your <span class="yellow-border-font">Challenges</span></h1></div>
      <div class="background-color-blue"><%= link_to "Add new challenge", new_challenge_path%></div>
      <% @challenges.each do |challenge| %>
      <div class="card">
        <div class="challenge">
          <div class="progress"><%= image_tag "goal_circle.png" %></div>
          <div class="challenge-summary">
            <h2><%= challenge.name %></h2>
            <p><%= challenge.category %><%= challenge.challenge_qty %><%= challenge.unit %> per week</p>
            <ul class="list-group">
              <% challenge.participations.each do |participation| %>
                <li class="list-group-item">
                          <%= cl_image_tag participation.user.photo.key, class: "avatar" %>
                  </li>
              <% end %>
            </ul>
          </div>
        </div>
        <%= link_to "challenge detail", challenge_path(challenge) %>

        <%# <% challenge.participations.each do |participation| %>
          <%# <%= participation.user.name %>
        <%# <% end %>
        <%# <%= challenge.weekly_progress.progress / challenge.challenge_qty %>
        <%# <%= challenge.weekly_progress.balance %>
      </div>
      <% end %>
    </div>
  </div>
</div>
