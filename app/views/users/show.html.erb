<div class= "card avatar-name">
<%= cl_image_tag @user.photo.key, crop: :fill, class: "avatar-profile" %>
 <h1 style="font-size:20vw"><%= @user.first_name %></h1>
</div>
<div class= "card">
  <h2>BIO</h2>

  <p><%= @user.bio %></p>
</div>
<div class= "card">
  <h2>Your Challenges</h2>
  <p>Ran 5k this week%</p>
  <p>Biked 10k this week</p>
</div>
 <%= link_to "edit", edit_user_registration_path(@user), class: "btn btn-ghost" %>
<%# <%= cl_image_tag("THE_IMAGE_ID_FROM_LIBRARY",
      width: 400, height: 300, crop: :fill) %>

<!-- for face detection -->
<%# <%= cl_image_tag("IMAGE_WITH_FACE_ID",
      width: 150, height: 150, crop: :thumb, gravity: :face) %>


<%# if @user == current user, then show  %>

<%= link_to image_tag("btn_strava_connectwith_orange.png"), @redirect_url  %>

<% @user.workouts.each do |workout| %>
  <%= workout.category %>
  <%= workout.distance %>
  <%= workout.duration %>
  <%= workout.date %>
  <br>
<% end %>
